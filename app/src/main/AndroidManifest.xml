<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.anastasiakantor.pressurecalcapp">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_ruby_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ruby_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.PressureCalcApp">
        <activity
            android:name="se.anastasiakantor.pressurecalcapp.MainActivity"
            android:windowSoftInputMode="adjustPan"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
